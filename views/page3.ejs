<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quiz</title>
  <link href="https://fonts.googleapis.com/css2?family=Exo:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      background-color: #242424;
      font-family: 'Exo', sans-serif;
      color: #00cdaf;
    }
    .card {
      width: 450px;
      background-color: #141414;
      border: 1px solid #00cdaf;
      box-shadow: 0 0 10px #00cdaf;
      color: #00cdaf;
      margin: auto;
      padding: 16px;
      border-radius: 8px;
    }
    .input-field {
      width: 100%;
      padding: 8px;
      margin-bottom: 16px;
      background-color: #141414;
      color: #00cdaf;
      border: 1px solid #00cdaf;
      border-radius: 8px;
    }
    .button {
      width: 100%;
      background-color: #00cdaf;
      color: #141414;
      padding: 10px;
      border-radius: 8px;
      text-align: center;
      cursor: pointer;
    }
    .message {
      text-align: center;
      margin-top: 16px;
    }
  </style>
</head>
<body>
  <div class="flex items-center justify-center min-h-screen">
    <div class="card">
      <div class="card-header text-center">
        <h2 class="text-2xl font-bold mb-4">In a best case scenario, how many tosses are needed to reach 100?</h2>
      </div>
      <div class="card-content">
        <img
          src="/images/image1.jpg"
          alt="Snake and Ladder game board"
          class="w-full h-auto mb-4 border"
          style="border-color: #00cdaf;
                 max-width:100%;"
        />
        <form id="quizForm" action="/kbVtxXADjx24noayRNSwEyeWxQU6yv" method="post">
          <input
            type="text"
            id="answerInput"
            class="input-field"
            name="key3"
            placeholder="Enter your answer"
          />
          <input type="submit" class="button" onclick="handleSubmit()" value="Submit Answer">
          <% if (typeof error !== 'undefined') { %>
            <script>alert("Sorry, that's not correct. Try again!")</script>
        <% } %>
        </form>
      </div>
      <div class="card-footer text-center">
        <p id="resultMessage" class="message"></p>
      </div>
    </div>
  </div>
</body>
</html>
